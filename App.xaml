<Application x:Class="EasyShare.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <!-- ===================================================================
             OBSIDIAN STUDIO DESIGN SYSTEM - EasyShare PRO
             עיצוב פרימיום כהה, אלגנטי ומודרני בתקן נגישות מחמיר WCAG AAA
             =================================================================== -->

        <!-- =================== צבעי רקע ומשטחים (Surfaces) =================== -->
        <!-- צבע רקע ראשי של האפליקציה: פחם נייטרלי אלגנטי Windows 11 Fluent Dark (ללא גווני כחול בוציים) -->
        <Color x:Key="ColorBackground">#1F1F1F</Color>
        <!-- צבע משטחי ביניים וסרגלים -->
        <Color x:Key="ColorSurface">#252525</Color>
        <!-- צבע משטח כרטיס יוקרתי (Elevated Card - Titanium) -->
        <Color x:Key="ColorCard">#2B2B2B</Color>
        <!-- צבע כרטיס במצב ריחוף (Card Hover) -->
        <Color x:Key="ColorCardHover">#323232</Color>
        <!-- גבול עדין מפריד משטחים (Neutral Subtle Border) -->
        <Color x:Key="ColorBorder">#383838</Color>
        <!-- גבול מודגש עם שקיפות (Active / Focused Border) -->
        <Color x:Key="ColorBorderHighlight">#0078D4</Color>

        <!-- =================== צבעי דגש (Accents) =================== -->
        <!-- צבע מותג כחול מערכת מעודן Windows 11 -->
        <Color x:Key="ColorPrimary">#0078D4</Color>
        <!-- צבע מותג ראשי במצב ריחוף / לחיצה -->
        <Color x:Key="ColorPrimaryHover">#0067C0</Color>
        <!-- דגש כחול רך אלגנטי ומאופק -->
        <Color x:Key="ColorAccentCyan">#388BE8</Color>
        <!-- דגש אינדיגו אלגנטי מעודן -->
        <Color x:Key="ColorAccentIndigo">#5B64E6</Color>

        <!-- =================== צבעי מערכת סמנטיים (WCAG AAA) =================== -->
        <!-- הצלחה / מחובר / פעיל -->
        <Color x:Key="ColorSuccess">#22C55E</Color>
        <!-- אזהרה / בתהליך -->
        <Color x:Key="ColorWarning">#F59E0B</Color>
        <!-- שגיאה / סכנה / מנותק -->
        <Color x:Key="ColorDanger">#E11D48</Color>

        <!-- =================== טיפוגרפיה (Typography) =================== -->
        <!-- טקסט ראשי קריא ובוהק -->
        <Color x:Key="ColorText">#FFFFFF</Color>
        <!-- טקסט משני בניגודיות גבוהה מעל 7:1 (אפור רך ונעים) -->
        <Color x:Key="ColorTextMuted">#B0B0B0</Color>
        <!-- טקסט עמום / רמזים / פלייסהולדרים -->
        <Color x:Key="ColorTextSubtle">#737373</Color>

        <!-- =================== מברשות צבע אחיד (SolidColorBrushes) =================== -->
        <SolidColorBrush x:Key="BrushBackground" Color="{StaticResource ColorBackground}" />
        <SolidColorBrush x:Key="BrushSurface" Color="{StaticResource ColorSurface}" />
        <SolidColorBrush x:Key="BrushCard" Color="{StaticResource ColorCard}" />
        <SolidColorBrush x:Key="BrushCardHover" Color="{StaticResource ColorCardHover}" />
        <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}" />
        <SolidColorBrush x:Key="BrushBorderHighlight" Color="{StaticResource ColorBorderHighlight}" Opacity="0.5" />
        <SolidColorBrush x:Key="BrushPrimary" Color="{StaticResource ColorPrimary}" />
        <SolidColorBrush x:Key="BrushPrimaryHover" Color="{StaticResource ColorPrimaryHover}" />
        <SolidColorBrush x:Key="BrushAccentCyan" Color="{StaticResource ColorAccentCyan}" />
        <SolidColorBrush x:Key="BrushAccentIndigo" Color="{StaticResource ColorAccentIndigo}" />
        <SolidColorBrush x:Key="BrushSuccess" Color="{StaticResource ColorSuccess}" />
        <SolidColorBrush x:Key="BrushWarning" Color="{StaticResource ColorWarning}" />
        <SolidColorBrush x:Key="BrushDanger" Color="{StaticResource ColorDanger}" />
        <SolidColorBrush x:Key="BrushText" Color="{StaticResource ColorText}" />
        <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource ColorTextMuted}" />
        <SolidColorBrush x:Key="BrushTextSubtle" Color="{StaticResource ColorTextSubtle}" />

        <!-- =================== מברשות גרדיאנט פרימיום (Gradients) =================== -->
        <!-- קו שפה עליון פנימי זוהר (Inner Top Hairline) להענקת עומק זכוכית יוקרתי -->
        <LinearGradientBrush x:Key="BrushInnerTopHairline" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#10FFFFFF" Offset="0.0" />
            <GradientStop Color="#00FFFFFF" Offset="1.0" />
        </LinearGradientBrush>

        <!-- גרדיאנט רקע אלגנטי לכרטיסים (Card Gradient) פחם וטיטניום נייטרליים -->
        <LinearGradientBrush x:Key="BrushCardGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#2D2D2D" Offset="0.0" />
            <GradientStop Color="#282828" Offset="1.0" />
        </LinearGradientBrush>

        <!-- גרדיאנט מותג כפתורים ראשיים (Primary Gradient) כחול מערכת Fluent -->
        <LinearGradientBrush x:Key="BrushPrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0078D4" Offset="0.0" />
            <GradientStop Color="#0067C0" Offset="1.0" />
        </LinearGradientBrush>

        <!-- גרדיאנט הדגשה מודרני מציאן לאינדיגו (Accent Gradient) -->
        <LinearGradientBrush x:Key="BrushAccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#388BE8" Offset="0.0" />
            <GradientStop Color="#5B64E6" Offset="1.0" />
        </LinearGradientBrush>

        <!-- שכבת הדגשת זכוכית (Glass Highlight) -->
        <LinearGradientBrush x:Key="BrushGlassHighlight" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#14FFFFFF" Offset="0.0" />
            <GradientStop Color="#04FFFFFF" Offset="1.0" />
        </LinearGradientBrush>

        <!-- =================== אפקטי צל והילה (Shadow & Glow Effects) =================== -->
        <!-- צל כרטיסים עדין ואלגנטי -->
        <DropShadowEffect x:Key="EffectCardShadow" BlurRadius="16" Direction="270" ShadowDepth="3" Opacity="0.35" Color="#000000" />
        <!-- צל שכבות מוגבהות וחלונות מודאליים -->
        <DropShadowEffect x:Key="EffectElevatedShadow" BlurRadius="24" Direction="270" ShadowDepth="6" Opacity="0.5" Color="#000000" />
        <!-- הילת אור כחולה סביב אלמנטים נבחרים -->
        <DropShadowEffect x:Key="EffectGlowPrimary" BlurRadius="12" Direction="0" ShadowDepth="0" Opacity="0.3" Color="#0078D4" />
        <!-- הילת ציאן חיה לאינדיקטורים ודגשים -->
        <DropShadowEffect x:Key="EffectGlowCyan" BlurRadius="12" Direction="0" ShadowDepth="0" Opacity="0.3" Color="#388BE8" />

        <!-- =================== סגנונות גלובליים (Global Styles) =================== -->

        <!-- סגנון פסי גלילה מודרניים דקים (Obsidian Studio ScrollBar) -->
        <Style x:Key="ObsidianScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="{StaticResource BrushBorder}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BrushAccentCyan}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter Property="Background" Value="{StaticResource BrushPrimary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="8" />
                    <Setter Property="Height" Value="Auto" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid Background="Transparent">
                                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ObsidianScrollBarThumb}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="8" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid Background="Transparent">
                                    <Track x:Name="PART_Track">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ObsidianScrollBarThumb}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- סגנון ToolTip יוקרתי בעיצוב כהה -->
        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Background" Value="{StaticResource BrushCard}" />
            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToolTip}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True"
                                Effect="{StaticResource EffectCardShadow}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- סגנון ContextMenu יוקרתי במצב כהה -->
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="{StaticResource BrushCard}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="4"
                                Effect="{StaticResource EffectElevatedShadow}"
                                SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- סגנון פריטי תפריט (MenuItem) -->
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Icon" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" Margin="16,0,0,0" Foreground="{StaticResource BrushTextMuted}" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource BrushCardHover}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource BrushTextSubtle}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
